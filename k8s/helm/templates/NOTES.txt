Check pod status with the command:

POD_NAME=$(kubectl get pods --show-all --namespace {{ .Release.Namespace }} -l "app={{ template "name" . }}" -o jsonpath="{.items[0].metadata.name}") && kubectl logs $POD_NAME --follow --namespace {{ .Release.Namespace }}
