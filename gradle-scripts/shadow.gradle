apply plugin: 'com.github.johnrengelman.shadow'
shadowJar {
    baseName = 'category-sync'
    classifier = null
    version = null
    manifest {
        inheritFrom rootProject.tasks.jar.manifest
    }
    doLast {
        println "Packaged JAR to ${destinationDir}/${archiveName}"
    }
}